# to read file from up to down 
    use cat and 
    use tac to view down to up view

# viewing file but without using much memory
    $ less somefile
    $ cat somefile | less

# to view most recent log file lines
    tail -n 15 somefile.log

