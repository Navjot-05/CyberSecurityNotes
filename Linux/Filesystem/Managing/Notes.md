# To find diff betweeen two files
    diff [options] <file1> <file2>
    to find diff between three files use diff3 instead of the diff
# Use 
    file * to find the type of the listed items of that folder