# Terminate Processes
    kill -SIGKILL <pid(process id)>
        OR
    kill -9 <pid>

# We can set the Priority of the processes
    by setting the nice value
    low value == Important
    higher value == less important